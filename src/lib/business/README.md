# Business mode helpers

Pure mapping utilities live in `modes.ts`.

Server-only capability loaders and enforcement live in `capabilities-server.ts`.

Never trust browser-submitted `dashboard_mode` or `business_id` without
membership / admin checks.
