# Shared dashboard components

Mode-agnostic dashboard pieces for both **hospitality** and **appointments**.

Examples of future residents: shell chrome, metric cards, chart primitives,
booking search, and status action panels.

Nothing is moved here in Phase 1. See `docs/hospitality-dashboard-baseline.md`.
